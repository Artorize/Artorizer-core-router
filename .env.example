# Server Configuration
PORT=7000
HOST=127.0.0.1
NODE_ENV=development
WORKERS=auto  # Use 'auto' to detect CPU cores, or specify a number (default: 4)

# Redis (for Bull queue - future use)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Processor Core API
PROCESSOR_URL=http://localhost:8000
PROCESSOR_TIMEOUT=30000

# Backend Storage API (handles all database operations)
# Note: Uses dynamic one-time tokens for authentication (no static token needed)
BACKEND_URL=http://localhost:5001
BACKEND_TIMEOUT=30000

# Router Configuration
# ROUTER_BASE_URL must match the public URL where users access the application
# This is used by Better Auth (on backend) to construct OAuth callback URIs
# Production example: https://router.artorizer.com
# The backend will create OAuth callback URIs as:
#   - {ROUTER_BASE_URL}/auth/callback/google
#   - {ROUTER_BASE_URL}/auth/callback/github
# You MUST register these exact callback URIs with Google and GitHub OAuth consoles
ROUTER_BASE_URL=http://localhost:7000
CALLBACK_AUTH_TOKEN=change-this-to-a-secure-random-token

# Rate Limiting
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=60000

# File Upload Limits
MAX_FILE_SIZE=268435456

# Self-Update Configuration
AUTO_UPDATE_ENABLED=true  # Set to 'false' to disable automatic updates on startup

# Authentication Configuration
# Router delegates all authentication to the backend (MongoDB-based sessions)
# Set AUTH_ENABLED=true to enable authentication proxy routes
AUTH_ENABLED=false

# CORS - Frontend origins that can make authenticated requests
# Comma-separated list of allowed origins
# REQUIRED when AUTH_ENABLED=true
ALLOWED_ORIGINS=https://artorizer.com,http://localhost:8080
